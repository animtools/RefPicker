# USER_GUIDE — RefPicker

RefPicker の使い方を、実際の UI 表記に合わせて説明するエンドユーザー向けマニュアルです。

- 対象 OS: Windows 10 / 11（64bit）
- 最短の始め方は [QUICKSTART.md](./QUICKSTART.md) を参照してください。

> **整備中**: 本文は開発リポジトリの詳細ガイドを基に、公開用に整理して拡充します。各節にはスクリーンショット（`./screenshots/`）を挿入予定です。

## 用語

- **ルート**: 索引対象として登録した画像フォルダ（配下を再帰的に索引）
- **絞り込みキーワード（フィルタ語）**: 上部の入力欄に Enter で登録する語句（複数可・OR 結合）
- **切り抜き**: 画像上に作った多角形／フリーハンド／矩形の領域
- **ピン**: 右サイドバーに固定する切り抜き（正規化プレビュー・対比表の入力にもなる）
- **対比表**: メジャー割当済みの切り抜きを共通ベースライン上に並べ、目盛り・名前ラベル・タイトルを焼き込んだ配布用 PNG

## 1. 資料フォルダを登録する（ルート）

1. 「設定…」→ **ルートフォルダ** を追加
2. 配下が再帰スキャンされ、一覧に画像が並ぶ
3. ファイルの追加・変更は監視され、自動で反映される

## 2. 参照を絞り込む

1. 上部の **絞り込みキーワード** に語句を入力して **Enter**（複数語は OR）
2. 一覧が即座に絞り込まれる
3. **履歴** からよく使う条件を復元できる
4. 見たくないフォルダ／ファイルは左ツリーの **探索から除外**（元ファイルは削除しない）

## 3. 切り抜きと書き出し

1. 切り抜きツール（多角形・フリーハンド・矩形）で必要部分を囲む（多角形は Enter で確定、Esc で取消）
2. 平滑化・ルーペで仕上げを調整
3. 切り抜きを選択し、**クリップボードへコピー**（他アプリへ貼り付け）／ **フォルダへ書き出し**

## 4. 分類タグで絞り込む

- 切り抜きに **分類タグ**（アングル・ショットサイズ・部位・表情）を付けておくと、
  「このくらいの向きの顔」「このくらいの寄り」という条件で絞り込める
- 参考になるかどうかを分けているのは多くの場合アングル。大量の資料から**その一枚**を引くための機能

> **分類タグで絞り込むのは無料**です（配られた資料に付いているタグでそのまま絞り込めます）。
> **分類タグを付ける／外す**には**個人ライセンス**が必要です。ご自身の言葉で付ける
> **自由なタグ**は無料のまま制限なく使えます。

## 5. メジャー正規化・対比表

- **メジャー** で基準を定義し、切り抜きへ割り当てて実寸を揃える
- **対比表** で、割当済みの切り抜きを共通ベースラインに並べた 1 枚 PNG を書き出す

> 書き出しには**個人ライセンス**が必要です（定義・プレビューまでは無料）。

## 6. プロジェクトの共有（配る／受け取る）

制作進行が資料一式を配り、アニメーターが受け取る仕組みです。

- **配る側（発行元）** — 設定の「プロジェクト」から **共有用に設定** し、共有先フォルダを選ぶ。
  以降は **共有先に更新を反映** で、変更のあったファイルだけが流れる
- **受け取る側** — **共有プロジェクトを読み込み** で取り込む。更新があれば通知され、1操作で適用できる
- 受け取った公式データは**読み取り専用**（タグの追加はできる）。進行側の正が守られる
- 取得する範囲は**ルート・フォルダ単位で選べる**（担当外の重い資料を抱えずに済む）

> **受け取る側にライセンスは要りません。**必要なのは配る側（**スタジオ用ライセンス**）です。
> ライセンスが失効しても、受け取った資料は引き続き使えます（停止するのは新規の公開のみ）。
> 詳しくは [LICENSE](./LICENSE) の「4. 料金とライセンス認証」を参照してください。

## 7. バージョンの確認

**設定 →「情報」タブ**に、バージョンとビルド時刻が表示されます（設定ウィンドウのタイトルバーにも出ます）。
不具合のご連絡には、この**ビルド時刻**を添えてください（[SUPPORT.md](./SUPPORT.md)）。

---

（各節は公開に向けて、スクリーンショットと具体的な操作手順を追記して仕上げます。）
