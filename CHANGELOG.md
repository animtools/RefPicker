# Changelog — RefPicker

このアプリのバージョンごとの変更内容です。

---

## [0.1.1] - 2026-09-14

### 変更

- **共有の更新は、確認のダイアログを出さずにそのまま取り込みます。** 受け取った側が切り抜きに追加したタグは常に残ります。共有元で切り抜きが消えたため残せなかったタグは、取り込みの完了時に件数でお知らせします。プロジェクトの切り替えから更新したときも同じです。
- 共有元が **「共有を終了…」** を実行したとき、受け取った側に「共有元がこの共有を終了しました」と表示するようにしました。
- **受け取ったプロジェクトは、共有元のデータを持ち出せないようにしました。** **「プロジェクトを書き出し…」**（移行）は使えません。あわせて「受け取りを停止して独立」をなくしました。受け取りをやめるときは、プロジェクトを削除してください。
- 共有フォルダの呼び名を、共有元・受け取った側のどちらでも「共有フォルダ」に揃えました。
- 自動切り抜きの設定で「インク」と呼んでいた項目を「描画」に改めました（**「描画しきい値」** など）。
- ライセンスが必要な操作で出るダイアログの文言を、画面の表記に合わせて「分類タグ」にしました。
- 資料を登録する前の画面の案内文を、実際の操作に合わせました（設定の開き方、キーワードの区切り方）。

### 修正

- スマートフォンで縦に撮った写真など、向きの情報が入った画像で、切り抜きの書き出しとクリップ正規化の結果が横倒しになっていたのを直しました。
- **「クリップ正規化ウィンドウを開く」** を押しても、最小化していたり他のウィンドウの後ろにあったりすると前に出てこなかったのを直しました。送る切り抜きが前回から変わっていれば、並びを初めから組み直します。
- 回転補正の **「鉛直に合わせる」** で、メジャーを上から下へ引いた切り抜きが上下逆さまになっていたのを直しました。
- メジャーに親子関係がある切り抜きに回転補正をかけると、配置が崩れていたのを直しました。

### 削除

- 設定の「切り抜き・タグ」タブにあった「切り抜き — 平滑化」を削除しました。どの操作にも使われていない設定でした。

### 訂正

- 配布 ZIP に同梱の `README.txt` で、データの保存先と更新手順の記述が実際と違っていたのを直しました。**更新は `RefPicker.exe` だけを上書き**してください。解凍先のフォルダごと消すと、同じフォルダの `RefPickerData` に入っているデータも消えます。

---

## [0.1.0] - 2026-09-07

Windows 64bit ポータブル版（ZIP を解凍して `RefPicker.exe` を起動）の、最初に配布したバージョンです。

### 主な機能

- 資料フォルダの索引化（再帰スキャン＋ファイルの追加・変更の自動検知）
- キーワードによる絞り込み（OR フィルタ）と履歴、探索からの除外
- 切り抜き（多角形・矩形）、ルーペ
- 切り抜きのクリップボードコピー／フォルダ書き出し
- メジャー正規化、対比表（PNG）書き出し
- **分類タグ**による絞り込み（顔の向き・ショット・部位・表情で参照を引く）
- **プロジェクトの共有** — 制作進行が資料一式を配り、アニメーターが受け取る（差分のみ転送）。
  **共有先はライセンス不要**
- **フリーミアム** — 無料で使える範囲と、個人／スタジオ用ライセンスで解除される機能を分けた
  （[LICENSE](./LICENSE) の「4. 料金とライセンス認証」参照）

---

※ 表示バージョンの正は**アプリ内 設定 →「情報」タブ**です（ビルド時刻も併記されます）。
